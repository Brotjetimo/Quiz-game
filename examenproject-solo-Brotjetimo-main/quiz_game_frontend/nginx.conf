# Makes sure to serve the index.html file for any route, allowing the frontend router to handle routing

server {
  listen 80;

  root /usr/share/nginx/html;
  index index.html;

  location / {
    try_files $uri /index.html;
  }
}